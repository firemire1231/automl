
<!DOCTYPE html>
<html>
    <head>
        <title>Credit Card Default Classifier</title>
    </head>
    <body>
        <form action =  "/" method = "POST" name = 'Parameters form'>
            <fieldset>
                <legend>Model Parameters:</legend>

                <label for="resampling">1. Choose resampling method:</label> <br><br>
                <select id="resampling" name="resampling" size="3">
                    <option value="Oversampling">Oversampling</option>
                    <option value="Undersampling">Undersampling</option>
                    <option value="No resampling">No resampling</option>
                </select> <br><br>

                <label for="features">2. Choose features:</label> <br><br>
                <select multiple id="features" name="features" size="12">
                    {% for elem in features %}
                        <option value={{elem}}>{{elem}}</option>
                    {% endfor %}
                </select> <br><br>

                <label for="scaling">3. Choose scaling method:</label> <br><br>
                <select id="scaling" name="scaling" size="3">
                    <option value="MinMax">MinMax</option>
                    <option value="Standard">Standard</option>
                    <option value="No scaling">No scaling</option>
                </select> <br><br>

                <label for="model">4. Choose model:</label> <br><br>
                <select id="model" name="model" size="4">
                    <option value="decision tree">decision tree</option>
                    <option value="knn">knn</option>
                    <option value="logistic regression">logistic regression</option>
                    <option value="random forest">random forest</option>
                </select> <br><br>

                <input id="upload" type="file">
                <form id="csvForm">
                        <input id="a"></input>
                        <input id="b"></input>
                        <input id="c"></input>
                        <input id="d"></input>
                </form>
                
                <input type="submit"  value = "Submit"> 
            </fieldset>

        </form> <br>
     
        <fieldset>
            <legend>Model Results:</legend>
            <p>Accuracy: {{ accuracy }}% </p> 
            <p>Precision: {{ precision }}% </p>
            <p>Recall: {{ recall }}% </p>
            <p>F1: {{ f1 }}% </p>
            <p>ROC_AUC: {{ roc_auc }}% </p>
        </fieldset><br>

        <p>
            API will return:  <br>
            {{ results }}
        </p> <br>

        <fieldset>
            <legend>Plot:</legend>
            <img src= "{{url_for('static', filename= 'plot.png')}}", height="600" >
        </fieldset>


    </body>
</html>
